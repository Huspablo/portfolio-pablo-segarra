# Dependencies
node_modules

# Build output
dist

# Environment & secrets
.env
.env.*
.env.local

# Vercel
.vercel

# macOS
.DS_Store
**/.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Video comprimido incluido en el repo (4 MB — OK para GitHub/Vercel)
# Si quieres reemplazarlo, comprime con: ffmpeg -i original.mp4 -vf scale=1280:-2 -crf 28 -preset slow -movflags +faststart kbe.mp4

# Original source folder (not needed in the deployed repo)
Portfolio psegarra/

# Claude Code session data
.claude/
